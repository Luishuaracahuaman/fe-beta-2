export interface Supplier {
  identifier: number;  // Cambia a solo number
  companyName: string;
  ruc: string;
  phone: string;
  address: string;
  email: string;
  state: string;
  registerDate: string;
}